//
//  Driver Card.js
//  Kite
//
//  Created on 6/21/17.
//

// ------------------------------------------
// Called just before the presentation starts
// ------------------------------------------

/*
layer.canvasReady = function(event) {
    
    for (var animation of this.animations) {
        
        animation.beginTime = 0;
    }
    
    this.lastDeltaY = 0;
};


// ------------------------------------------
// Handle Events
// ------------------------------------------

layer.mouseUp = function(event) {

    var lastDeltaY = this.lastDeltaY;
    
    if (lastDeltaY == 0) { return; }
    
    else if (lastDeltaY < 0)
    {
        this.addAnimationForKey(this.animations[1], "open");
    }
    else if (lastDeltaY > 0)
    {
        this.addAnimationForKey(this.animations[2], "close");
    }
    
    this.lastDeltaY = 0;
};

layer.mouseDragged = function(event) {

    this.lastDeltaY = event.deltaY;
};
*/